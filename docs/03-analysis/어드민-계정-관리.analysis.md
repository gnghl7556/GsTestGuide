# 어드민-계정-관리 Analysis Report

> **Analysis Type**: Gap Analysis (Design vs Implementation)
>
> **Project**: GS Test Guide
> **Analyst**: gap-detector (claude-opus-4-6)
> **Date**: 2026-02-10
> **Design Doc**: [어드민-계정-관리.design.md](../02-design/features/어드민-계정-관리.design.md)

---

## 1. Analysis Overview

### 1.1 Analysis Purpose

설계 문서(어드민-계정-관리.design.md)와 실제 구현 코드 사이의 일치도를 측정하고,
누락/추가/변경된 항목을 식별하여 설계-구현 동기화 상태를 검증한다.

### 1.2 Analysis Scope

- **Design Document**: `docs/02-design/features/어드민-계정-관리.design.md`
- **Implementation Path**: `src/features/admin/`, `src/routes.tsx`, `src/features/test-setup/routes/OverviewPage.tsx`, `.env`
- **Analysis Date**: 2026-02-10

---

## 2. Gap Analysis (Design vs Implementation)

### 2.1 Section 1: Architecture Overview (설계 원칙 + 데이터 흐름)

| Design Item | Implementation | Status | Notes |
|-------------|---------------|:------:|-------|
| 독립 모듈 `src/features/admin/` | `src/features/admin/{hooks,components,routes}/` | Match | 모든 어드민 코드 격리됨 |
| 기존 라우트 변경 없음 | `src/routes.tsx` 기존 라우트 유지 | Match | 기존 5개 라우트 그대로 |
| 기존 훅 재사용 (`useDirectoryActions` 등) | UserManagement: `useTestSetupContext()` 사용 | Match | Context 통해 CRUD 접근 |
| UI 일관성 (Tailwind + Button/Input) | Button 컴포넌트 재사용, Tailwind 패턴 동일 | Match | |
| 데이터 흐름: 대시보드 -> 모달 -> /admin | OverviewPage -> AdminPasswordModal -> navigate('/admin') | Match | |

**Section 1 Score: 5/5 (100%)**

---

### 2.2 Section 2: Component Detail Design

#### 2.2.1 useAdminAuth Hook

| Design Item | Implementation | Status | Notes |
|-------------|---------------|:------:|-------|
| File path: `src/features/admin/hooks/useAdminAuth.ts` | 동일 | Match | |
| `ADMIN_KEY = 'gs-admin-authenticated'` | `const ADMIN_KEY = 'gs-admin-authenticated'` | Match | 완전 일치 |
| `useState` 초기값 sessionStorage 체크 | `() => sessionStorage.getItem(ADMIN_KEY) === 'true'` | Match | 완전 일치 |
| `authenticate(password)` -> env 비교 -> sessionStorage 저장 | 동일 로직 | Match | 완전 일치 |
| `logout()` -> sessionStorage 제거 | 동일 로직 | Match | 완전 일치 |
| return `{ isAdmin, authenticate, logout }` | 동일 | Match | 완전 일치 |

**useAdminAuth Score: 6/6 (100%)**

#### 2.2.2 AdminPasswordModal

| Design Item | Implementation | Status | Notes |
|-------------|---------------|:------:|-------|
| File path 일치 | 동일 | Match | |
| Props: `{ open, onClose, onSuccess }` | 동일 | Match | |
| 타이틀 "관리자 인증" | `관리자 인증` | Match | |
| 비밀번호 라벨 | `비밀번호` | Match | |
| password input (autoFocus) | `setTimeout(() => inputRef.current?.focus(), 50)` | Match | autoFocus를 ref+timeout으로 구현 (기능적 동등) |
| 에러 메시지 조건부 표시 | `{error && <p>...{error}</p>}` | Match | |
| 취소/확인 버튼 | 취소 + 확인 버튼 존재 | Match | |
| Enter 키 제출 | `onKeyDown: if (e.key === 'Enter') handleSubmit()` | Match | |
| Escape 키 -> onClose | `document.addEventListener('keydown', handleEsc)` | Match | |
| 실패 시 에러 표시 + input 초기화 | `setError(...)`, `setPassword('')`, `inputRef.current?.focus()` | Match | |
| 배경 딤 | `bg-black/30` | Match | |
| 빈 입력 검증 | `if (!password.trim())` -> 에러 메시지 | Extra | 설계에 명시 안됨, 양호한 추가 |

**AdminPasswordModal Score: 11/11 (100%) + 1 Extra**

#### 2.2.3 AdminGuard

| Design Item | Implementation | Status | Notes |
|-------------|---------------|:------:|-------|
| File path 일치 | 동일 | Match | |
| `useAdminAuth().isAdmin` 체크 | 동일 | Match | |
| 미인증 시 `<Navigate to="/dashboard" replace />` | 동일 | Match | |
| 인증 시 `<Outlet />` | 동일 | Match | |

**AdminGuard Score: 4/4 (100%)**

#### 2.2.4 AdminLayout

| Design Item | Implementation | Status | Notes |
|-------------|---------------|:------:|-------|
| File path 일치 | 동일 | Match | |
| 사이드바 + Outlet 구조 | `<aside>` + `<main><Outlet/></main>` | Match | |
| NAV_ITEMS: `/admin/users` (Users icon) | 동일 path, label, icon | Match | |
| NAV_ITEMS: `/admin/projects` (FolderKanban icon) | 동일 path, label, icon | Match | |
| 돌아가기 버튼 -> `/dashboard` | `navigate('/dashboard')` with ArrowLeft icon | Match | |
| 타이틀 "GS 시험 가이드 - 관리자" (상단 바 중앙) | 없음 | Missing | 상단 바에 타이틀 텍스트 미구현 |
| 로그아웃 버튼 (우측) -> logout + navigate | `handleLogout()`: `logout()` + `navigate('/dashboard')` | Match | 사이드바 하단에 배치 |
| 버튼 라벨 "관리자 나가기" | `관리자 나가기` | Match | |
| 상단 바 레이아웃 (좌: 돌아가기, 중앙: 타이틀, 우: 나가기) | 사이드바 내에 돌아가기(상단) + 나가기(하단) | Changed | 설계: 가로 상단바 / 구현: 사이드바 내 배치 |

**AdminLayout Score: 7/9 (78%)**

- Missing: 타이틀 "GS 시험 가이드 - 관리자" 미표시
- Changed: 상단 바 레이아웃 -> 사이드바 내부 배치로 변경 (기능적으로 동등, UI 배치 차이)

#### 2.2.5 UserManagement

| Design Item | Implementation | Status | Notes |
|-------------|---------------|:------:|-------|
| File path 일치 | 동일 | Match | |
| `useTestSetupContext()` -> users, CRUD | `const { users, createUser, updateUser, deleteUser }` | Match | |
| 테이블 칼럼: 이름, 직급, 이메일, 연락처, 작업 | 이름/직급/이메일/연락처/작업 (5칼럼) | Match | |
| 사용자 추가 버튼 | `<Button> <Plus/> 사용자 추가</Button>` | Match | |
| 인라인 폼으로 추가 | 테이블 상단 인라인 행 | Match | |
| 사용자 수정 (인라인 편집) | editingId 기반 인라인 편집 | Match | |
| 사용자 삭제 (확인 팝업) | deleteConfirmId 기반 인라인 확인 | Match | |
| 폼 필드: 이름(text), 직급(select), 이메일(email), 연락처(tel) | 이름(input), 직급(select), 이메일(input), 연락처(input) | Match | input type은 text이나 기능 동등 |
| 직급 옵션: 전임/선임/책임/수석 | `RANK_OPTIONS: ['전임', '선임', '책임', '수석']` | Match | |
| 역할(Tester 등) 칼럼 | 미구현 | Missing | 설계 테이블에 "역할" 칼럼 있으나 구현에 없음 |
| overflow-x-auto (가로 스크롤) | `<div className="overflow-x-auto">` | Match | |

**UserManagement Score: 10/11 (91%)**

- Missing: "역할" 칼럼 미구현 (설계 UI 구조에 표시됨)

#### 2.2.6 ProjectManagement

| Design Item | Implementation | Status | Notes |
|-------------|---------------|:------:|-------|
| File path 일치 | 동일 | Match | |
| `useTestSetupContext()` -> projects, users | `const { projects, users }` | Match | |
| 테이블 칼럼: 시험번호, 제품명, 업체, 상태, 시험원, 작업 | 6칼럼 모두 일치 | Match | |
| 프로젝트 생성 버튼 | `<Button> <Plus/> 프로젝트 생성</Button>` | Match | |
| 프로젝트 생성 (기존 `createProjectFromInput` 활용) | Firestore `setDoc` 직접 호출 | Changed | 기존 함수 미사용, 직접 Firestore 쓰기 |
| 시험원 배정 (드롭다운 -> users 목록) | `assigningId` 기반 드롭다운 select | Match | |
| 배정 시 Firestore 업데이트 (testerId, testerName, testerPhone, testerEmail) | `setDoc(merge)` with 동일 필드 | Match | |
| 상태 변경 (대기/진행/중단/완료/재시험) | `STATUS_OPTIONS: ['대기', '진행', '중단', '완료', '재시험']` | Match | |
| 프로젝트 삭제 (확인 팝업) | `deleteConfirmId` 기반 인라인 확인 | Match | |
| Firestore 직접 CRUD | `setDoc`, `deleteDoc` from firebase/firestore | Match | |
| overflow-x-auto | `<div className="overflow-x-auto">` | Match | |

**ProjectManagement Score: 10/11 (91%)**

- Changed: `createProjectFromInput` 미사용, Firestore 직접 작성 (기능 동등하나 재사용 원칙과 불일치)

---

### 2.3 Section 3: Route Design

| Design Item | Implementation | Status | Notes |
|-------------|---------------|:------:|-------|
| 기존 라우트 유지 (index, dashboard, design, execution, report) | 모두 유지 | Match | |
| `<AdminGuard>` 래핑 | `<Route element={<AdminGuard />}>` | Match | |
| `/admin` -> `<AdminLayout />` | `<Route path="admin" element={<AdminLayout />}>` | Match | |
| `/admin` index -> `Navigate to="users"` | `<Route index element={<Navigate to="users" replace />}>` | Match | |
| `/admin/users` -> `<UserManagement />` | 동일 | Match | |
| `/admin/projects` -> `<ProjectManagement />` | 동일 | Match | |
| catch-all `*` -> `/dashboard` | `<Route path="*" element={<Navigate to="/dashboard" replace />}>` | Match | |

**Section 3 Score: 7/7 (100%)**

---

### 2.4 Section 4: Environment Variables

| Design Item | Implementation | Status | Notes |
|-------------|---------------|:------:|-------|
| `.env`에 `VITE_ADMIN_PASSWORD=12Sqecd34!` | `.env` line 7: `VITE_ADMIN_PASSWORD=12Sqecd34!` | Match | |
| `env.d.ts`에 `ImportMetaEnv` 타입 추가 | `vite-env.d.ts` 또는 `env.d.ts` 파일 없음 | Missing | 타입 선언 미구현 |
| `.env.example`에 반영 | `.env.example`에 `VITE_ADMIN_PASSWORD` 미포함 | Missing | 템플릿 업데이트 누락 |

**Section 4 Score: 1/3 (33%)**

---

### 2.5 Section 5: Admin Button Placement

| Design Item | Implementation | Status | Notes |
|-------------|---------------|:------:|-------|
| GlobalProcessHeader에 관리자 버튼 추가 | GlobalProcessHeader에 `onOpenAdmin` prop 미추가 | Missing | 설계와 다른 위치에 구현 |
| OverviewPage에서도 버튼 배치 | OverviewPage에 관리자 버튼 + 모달 통합 | Match | |
| Settings 아이콘 + "관리자" 텍스트 | `<Settings size={14} />` + `<span>관리자</span>` | Match | |
| aria-label, title 속성 | 둘 다 "관리자 페이지" | Match | |
| 반응형 (lg:inline) | `hidden sm:inline` (sm 기준) | Changed | 설계: lg / 구현: sm |

**Section 5 Score: 3/5 (60%)**

- Missing: GlobalProcessHeader에는 관리자 버튼 미추가 (대시보드에서만 접근 가능)
- Changed: 반응형 breakpoint (lg -> sm)

---

### 2.6 Section 6: File List & Implementation Order

| Design File | Exists | Status | Notes |
|-------------|:------:|:------:|-------|
| `.env` (VITE_ADMIN_PASSWORD) | Yes | Match | |
| `src/features/admin/hooks/useAdminAuth.ts` | Yes | Match | |
| `src/features/admin/components/AdminPasswordModal.tsx` | Yes | Match | |
| `src/features/admin/routes/AdminGuard.tsx` | Yes | Match | |
| `src/features/admin/components/AdminLayout.tsx` | Yes | Match | |
| `src/routes.tsx` (수정) | Yes | Match | |
| 대시보드 영역 (관리자 버튼 통합) | Yes | Match | OverviewPage에 구현 |
| `src/features/admin/components/UserManagement.tsx` | Yes | Match | |
| `src/features/admin/components/ProjectManagement.tsx` | Yes | Match | |

**Section 6 Score: 9/9 (100%)**

---

### 2.7 Section 7: Styling Rules

| Design Item | Implementation | Status | Notes |
|-------------|---------------|:------:|-------|
| 사이드바 배경: `bg-slate-900` | `bg-slate-900` | Match | |
| 사이드바 텍스트: `text-slate-300`, 활성: `text-white bg-slate-800` | `text-slate-400` (비활성), `text-white bg-slate-800` (활성) | Partial | 비활성 색상 미세 차이 (300 vs 400) |
| 콘텐츠 영역: `bg-slate-50` | `bg-slate-50` | Match | |
| 테이블: border/rounded 패턴 | `rounded-xl border border-slate-200` | Match | |
| 기존 Button 재사용 | `import { Button } from '../../../components/ui'` | Match | |
| overflow-x-auto | UserManagement, ProjectManagement 모두 적용 | Match | |

**Section 7 Score: 5.5/6 (92%)**

---

### 2.8 Section 8: Existing Code Reuse Mapping

| Design Reuse Target | Implementation | Status | Notes |
|---------------------|---------------|:------:|-------|
| `useDirectoryActions` -> 사용자 CRUD | `useTestSetupContext()` -> `createUser, updateUser, deleteUser` | Match | Context 경유 접근 |
| `useUsers` -> Context에서 users | `useTestSetupContext()` -> `users` | Match | |
| `useProjects` -> Context에서 projects | `useTestSetupContext()` -> `projects` | Match | |
| `createProjectFromInput` -> 프로젝트 생성 | Firestore `setDoc` 직접 호출 | Changed | 기존 함수 미사용 |
| `Button`, `Input` 컴포넌트 | `Button` 사용, `Input`은 미사용 (native input 직접) | Partial | Button만 재사용, Input은 native 사용 |
| 로그아웃 모달 스타일 패턴 | AdminPasswordModal이 동일 스타일 패턴 적용 | Match | |

**Section 8 Score: 4.5/6 (75%)**

---

## 3. Match Rate Summary

### 3.1 Section-wise Scores

| Section | Items | Matched | Score | Status |
|---------|:-----:|:-------:|:-----:|:------:|
| 1. Architecture Overview | 5 | 5 | 100% | Pass |
| 2.1 useAdminAuth | 6 | 6 | 100% | Pass |
| 2.2 AdminPasswordModal | 11 | 11 | 100% | Pass |
| 2.3 AdminGuard | 4 | 4 | 100% | Pass |
| 2.4 AdminLayout | 9 | 7 | 78% | Warn |
| 2.5 UserManagement | 11 | 10 | 91% | Pass |
| 2.6 ProjectManagement | 11 | 10 | 91% | Pass |
| 3. Route Design | 7 | 7 | 100% | Pass |
| 4. Environment Variables | 3 | 1 | 33% | Fail |
| 5. Admin Button Placement | 5 | 3 | 60% | Fail |
| 6. File List | 9 | 9 | 100% | Pass |
| 7. Styling Rules | 6 | 5.5 | 92% | Pass |
| 8. Code Reuse Mapping | 6 | 4.5 | 75% | Warn |

### 3.2 Overall Match Rate

```
+---------------------------------------------+
|  Overall Match Rate: 89% (83/93 items)      |
+---------------------------------------------+
|  Match:              75 items  (81%)        |
|  Partial/Changed:     8 items  ( 9%)        |
|  Missing:             7 items  ( 8%)        |
|  Extra:               3 items  ( 3%)        |
+---------------------------------------------+
```

---

## 4. Differences Found

### 4.1 Missing Features (Design O, Implementation X)

| # | Item | Design Location | Description | Severity |
|:-:|------|-----------------|-------------|:--------:|
| 1 | env.d.ts 타입 선언 | Section 4 (L306-311) | `ImportMetaEnv`에 `VITE_ADMIN_PASSWORD` 타입 미추가 | Low |
| 2 | .env.example 업데이트 | Section 4 (L298-302) | `.env.example`에 `VITE_ADMIN_PASSWORD=` 미추가 | Medium |
| 3 | GlobalProcessHeader 관리자 버튼 | Section 5 (L318-344) | GlobalProcessHeader에 `onOpenAdmin` prop 미추가 | Medium |
| 4 | AdminLayout 타이틀 텍스트 | Section 2.4 (L170) | "GS 시험 가이드 - 관리자" 타이틀 상단 미표시 | Low |
| 5 | UserManagement 역할 칼럼 | Section 2.5 (L188-189) | 테이블에 "역할" 칼럼 미구현 | Low |

### 4.2 Changed Features (Design != Implementation)

| # | Item | Design | Implementation | Impact |
|:-:|------|--------|----------------|:------:|
| 1 | AdminLayout 배치 | 상단 바 (좌: 돌아가기, 중앙: 타이틀, 우: 나가기) | 사이드바 내부 (상단: 돌아가기, 하단: 나가기) | Low |
| 2 | ProjectManagement 생성 | `createProjectFromInput` 함수 재사용 | Firestore `setDoc` 직접 호출 | Medium |
| 3 | Button breakpoint | `hidden lg:inline` | `hidden sm:inline` | Low |
| 4 | 사이드바 비활성 텍스트 | `text-slate-300` | `text-slate-400` | Low |
| 5 | Input 컴포넌트 재사용 | 기존 `Input` 컴포넌트 사용 | native `<input>` 직접 사용 | Low |

### 4.3 Extra Features (Design X, Implementation O)

| # | Item | Implementation Location | Description |
|:-:|------|------------------------|-------------|
| 1 | 빈 입력 검증 | AdminPasswordModal L36-39 | `password.trim()` 빈 값 체크 추가 |
| 2 | 사용자 수 표시 | UserManagement L64 | `({users.length}명)` 카운트 표시 |
| 3 | 프로젝트 수 표시 | ProjectManagement L78 | `({projects.length}건)` 카운트 표시 |

---

## 5. Architecture Compliance

### 5.1 Feature Module Isolation

| Check Item | Status | Notes |
|------------|:------:|-------|
| `src/features/admin/` 독립 모듈 | Pass | hooks, components, routes 하위 구조 |
| 기존 코드 변경 최소화 | Pass | routes.tsx에 import 추가 + OverviewPage에 버튼 추가만 |
| Context 경유 데이터 접근 | Pass | `useTestSetupContext()` 통해 접근 |

### 5.2 Dependency Direction

| File | Layer | Dependencies | Status |
|------|-------|-------------|:------:|
| useAdminAuth.ts | Hook (Application) | React only | Pass |
| AdminPasswordModal.tsx | Component (Presentation) | useAdminAuth hook | Pass |
| AdminGuard.tsx | Route Guard (Presentation) | useAdminAuth, react-router-dom | Pass |
| AdminLayout.tsx | Layout (Presentation) | useAdminAuth, react-router-dom, lucide-react | Pass |
| UserManagement.tsx | Feature (Presentation) | useTestSetupContext, Button UI | Pass |
| ProjectManagement.tsx | Feature (Presentation) | useTestSetupContext, firebase/firestore, Button UI | Warn |

- ProjectManagement.tsx가 `firebase/firestore`를 직접 import (Infrastructure 직접 참조). 이는 설계 의도와 무관하게 Clean Architecture 위반에 해당하나, 프로젝트 레벨(Starter~Dynamic)에서 허용 가능.

### 5.3 Architecture Score

```
+---------------------------------------------+
|  Architecture Compliance: 92%               |
+---------------------------------------------+
|  Correct layer placement: 8/9 files         |
|  Dependency violations:   1 file (minor)    |
|  Wrong layer:             0 files           |
+---------------------------------------------+
```

---

## 6. Convention Compliance

### 6.1 Naming Convention

| Category | Convention | Files Checked | Compliance | Violations |
|----------|-----------|:-------------:|:----------:|------------|
| Components | PascalCase | 6 | 100% | - |
| Functions | camelCase | 12 | 100% | - |
| Constants | UPPER_SNAKE_CASE | 5 | 100% | `ADMIN_KEY`, `NAV_ITEMS`, `RANK_OPTIONS`, `STATUS_OPTIONS`, `STATUS_COLORS` |
| Files (component) | PascalCase.tsx | 5 | 100% | - |
| Files (hook) | camelCase.ts | 1 | 100% | - |
| Folders | kebab-case | 3 | 100% | `admin/hooks/`, `admin/components/`, `admin/routes/` |

### 6.2 Import Order

| File | External First | Internal Second | Relative Third | Status |
|------|:--------------:|:--------------:|:--------------:|:------:|
| useAdminAuth.ts | react | - | - | Pass |
| AdminPasswordModal.tsx | react | - | ../hooks | Pass |
| AdminGuard.tsx | react-router-dom | - | ../hooks | Pass |
| AdminLayout.tsx | react-router-dom, lucide-react | - | ../hooks | Pass |
| UserManagement.tsx | react, lucide-react | - | ../../../providers, ../../../components | Pass |
| ProjectManagement.tsx | react, lucide-react, firebase | - | ../../../providers, ../../../lib | Pass |

### 6.3 Environment Variable Convention

| Variable | Convention | Actual | Status |
|----------|-----------|--------|:------:|
| `VITE_ADMIN_PASSWORD` | `VITE_*` prefix (client-exposed) | `VITE_ADMIN_PASSWORD` | Warn |

- `VITE_ADMIN_PASSWORD`는 client-exposed 변수이므로 브라우저 소스에서 확인 가능. 보안상 민감한 비밀번호가 클라이언트에 노출되나, 설계 문서에서 이를 의도적으로 수용한 것으로 판단.

### 6.4 Convention Score

```
+---------------------------------------------+
|  Convention Compliance: 96%                 |
+---------------------------------------------+
|  Naming:          100%                      |
|  Import Order:    100%                      |
|  File Structure:  100%                      |
|  Env Variables:    85%                      |
+---------------------------------------------+
```

---

## 7. Overall Score

```
+---------------------------------------------+
|  Overall Score: 89/100                      |
+---------------------------------------------+
|  Design Match:        89%                   |
|  Architecture:        92%                   |
|  Convention:          96%                   |
|  Combined Average:    92%                   |
+---------------------------------------------+
```

| Category | Score | Status |
|----------|:-----:|:------:|
| Design Match | 89% | Warn |
| Architecture Compliance | 92% | Pass |
| Convention Compliance | 96% | Pass |
| **Overall** | **92%** | **Pass** |

---

## 8. Recommended Actions

### 8.1 Immediate Actions (Priority High)

| # | Action | File | Description |
|:-:|--------|------|-------------|
| 1 | `.env.example` 업데이트 | `.env.example` | `VITE_ADMIN_PASSWORD=` 항목 추가 |

### 8.2 Short-term Actions (Priority Medium)

| # | Action | File | Description |
|:-:|--------|------|-------------|
| 1 | GlobalProcessHeader 관리자 버튼 | `src/components/Layout/GlobalProcessHeader.tsx` | 설계대로 `onOpenAdmin` prop 추가 (작업 페이지에서도 관리자 접근 가능하게) |
| 2 | `createProjectFromInput` 재사용 | `src/features/admin/components/ProjectManagement.tsx` | Firestore 직접 호출 대신 기존 함수 활용 검토 |

### 8.3 Optional Improvements (Priority Low)

| # | Action | File | Description |
|:-:|--------|------|-------------|
| 1 | env.d.ts 타입 선언 | `src/vite-env.d.ts` | `ImportMetaEnv`에 `VITE_ADMIN_PASSWORD` 추가 (타입 안전) |
| 2 | AdminLayout 타이틀 | `AdminLayout.tsx` | "GS 시험 가이드 - 관리자" 타이틀 표시 |
| 3 | UserManagement 역할 칼럼 | `UserManagement.tsx` | 역할 칼럼 추가 또는 설계 문서에서 제거 |

---

## 9. Design Document Updates Needed

설계 문서와 구현이 다르지만 구현이 더 적절한 항목:

- [ ] Section 2.4 AdminLayout: 상단 바 레이아웃 -> 사이드바 내부 배치로 변경 반영
- [ ] Section 5: 관리자 버튼 배치를 OverviewPage 전용으로 변경 (GlobalProcessHeader 미적용 반영)
- [ ] Section 5: 반응형 breakpoint `lg` -> `sm` 변경 반영
- [ ] Section 8: `Input` 컴포넌트 미사용 (native input 사용) 반영
- [ ] Section 2.5: "역할" 칼럼 제거 또는 추가 결정

---

## 10. Security Notes

| Item | Severity | Description |
|------|:--------:|-------------|
| Client-side password | Info | `VITE_ADMIN_PASSWORD`는 빌드 시 번들에 포함되어 클라이언트에서 접근 가능. 설계 문서에서 이를 인지하고 수용한 상태. 향후 서버 기반 인증으로 전환 권장. |
| `.env` in Git | Warn | `.env` 파일이 Git에 포함되어 있다면 Firebase API Key 및 Admin Password 노출 위험. `.gitignore`에 등록 확인 필요. |

---

## 11. Next Steps

- [ ] `.env.example`에 `VITE_ADMIN_PASSWORD=` 추가
- [ ] 설계 문서 업데이트 (Section 9 항목 반영)
- [ ] (선택) GlobalProcessHeader 관리자 버튼 추가 또는 설계 문서 수정
- [ ] 완료 후 Report 문서 작성: `/pdca report 어드민-계정-관리`

---

## Version History

| Version | Date | Changes | Author |
|---------|------|---------|--------|
| 0.1 | 2026-02-10 | Initial gap analysis | gap-detector |
